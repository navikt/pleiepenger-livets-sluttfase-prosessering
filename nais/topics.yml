apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: privat-pp-livets-sluttfase-mottatt
  namespace: dusseldorf
  labels:
    team: dusseldorf
spec:
  pool: {{kafka-pool}}
  config: # optional; all fields are optional too; defaults shown
    cleanupPolicy: delete  # delete, compact
    minimumInSyncReplicas: 2
    partitions: 1
    replication: 3  # see min/max requirements
    retentionBytes: -1  # -1 means unlimited
    retentionHours: 730  # -1 means unlimited
  acl:
    - team: dusseldorf
      application: pleiepenger-livets-sluttfase-api
      access: write
    - team: dusseldorf
      application: pleiepenger-livets-sluttfase-prosessering
      access: read

---

apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: privat-pp-livets-sluttfase-preprosessert
  namespace: dusseldorf
  labels:
    team: dusseldorf
spec:
  pool: {{kafka-pool}}
  config:  # optional; all fields are optional too; defaults shown
    cleanupPolicy: delete  # delete, compact
    minimumInSyncReplicas: 2
    partitions: 1
    replication: 3  # see min/max requirements
    retentionBytes: -1  # -1 means unlimited
    retentionHours: 730  # -1 means unlimited
  acl:
    - team: dusseldorf
      application: pleiepenger-livets-sluttfase-prosessering
      access: readwrite

---

apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: privat-pp-livets-sluttfase-cleanup
  namespace: dusseldorf
  labels:
    team: dusseldorf
spec:
  pool: {{kafka-pool}}
  config:  # optional; all fields are optional too; defaults shown
    cleanupPolicy: delete  # delete, compact
    minimumInSyncReplicas: 2
    partitions: 1
    replication: 3  # see min/max requirements
    retentionBytes: -1  # -1 means unlimited
    retentionHours: 730  # -1 means unlimited
  acl:
    - team: dusseldorf
      application: pleiepenger-livets-sluttfase-prosessering
      access: readwrite
